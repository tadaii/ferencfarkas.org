{
  "name": "ferencfarkas.org",
  "version": "0.1.0",
  "description": "The ferencfarkas.org project",
  "private": true,
  "scripts": {
    "web:start": "cd ./website && hugo server",
    "web:build": "cd ./website && hugo && npm run web:build:functions",
    "web:build:functions": "cd ./website && netlify-lambda build assets/lambda",
    "web:server": "cd ./website && hugo server",
    "catalogue:pdf": "node ./catalogue/pdf-generator/index.js"
  },
  "devDependencies": {
    "autoprefixer": "^9.6.1",
    "postcss-cli": "^6.1.3",
    "postcss-custom-media": "^7.0.8"
  },
  "dependencies": {
    "netlify-lambda": "^1.5.0",
    "pdfmake": "^0.1.58",
    "shelljs": "^0.8.3",
    "yaml": "^1.6.0"
  }
}
